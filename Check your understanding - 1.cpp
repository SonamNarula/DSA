#include <iostream>
using namespace std;

int main() {
    // Q1) How will you declare a character equal to exclamatory mark?
    char ch = '!';

    // Q2) Take two values a, b as input, and output three values: a+b and a*b and a/b a/b should be
    // a decimal, not an integer
    double a, b;
    cin >> a >> b;

    cout << a + b << endl;
    cout << a * b << endl;
    cout << a / b << endl;

    return 0;
}
